SELECT title FROM movies WHERE id IN (SELECT movie_id FROM stars JOIN people ON id = person_id WHERE name = 'Jennifer Lawrence' OR name = 'Bradley Cooper' GROUP BY movie_ID HAVING COUNT(movie_id) > 1);